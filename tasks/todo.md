# Task Plan
- [x] Fix edge-to-edge alignment rendering issues on the Navbar.
- [x] Replace basic mobile menu dropdown with a premium, staggered-animation full-screen overlay menu.
- [x] Fix `Team.tsx` rigid height properties to use `aspect-[4/5]` and `flex-grow` for perfectly uniform card sizes across varying screen widths.

# Review
- [x] All team images must render with identical heights instead of stretching inconsistently.
- [x] The Navbar must touch the viewport edges natively and elegantly overlay content.
- [x] The mobile hamburger must trigger a modern app-like full-screen frosted glass overlay.
